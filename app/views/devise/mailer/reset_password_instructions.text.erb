Xin chào <%= @resource.email %>!

Có người đã yêu cầu liên kết để thay đổi mật khẩu của bạn. Bạn có thể thực hiện điều này thông qua liên kết bên dưới.

<%= @resource.is_a?(User) ? "#{ENV.fetch('FRONTEND_URL', 'http://localhost:3000')}/dat-lai-mat-khau?token=#{@token}" : edit_admin_password_url(reset_password_token: @token) %>

Nếu bạn không yêu cầu điều này, vui lòng bỏ qua email này.
Mật khẩu của bạn sẽ không thay đổi cho đến khi bạn truy cập vào liên kết trên và tạo mật khẩu mới.
