
<div class="flex flex-col gap-4" data-controller="products-index">
  <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
    <div>
      <h1 class="text-2xl font-bold">Products</h1>
      <nav class="text-sm breadcrumbs mt-1">
        <ol class="inline-flex items-center space-x-1">
          <li>
            <%= link_to "Admin", admin_dashboard_path, class: "text-gray-500 hover:text-gray-700" %>
          </li>
          <li>/</li>
            <%= link_to "Products", admin_products_path, class: "text-gray-500 hover:text-gray-700" %>
          <li>/</li>
          <li class="text-gray-900">Create Product</li>
        </ol>
      </nav>
    </div>
  </div>
  <div class="max-full mx-auto space-y-4" data-controller="products-new">
    <%= render "form", product: @product %>
  </div>
</div>

