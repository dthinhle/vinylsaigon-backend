<div class="rounded-lg border border-gray-200 bg-white p-4 space-y-2">
  <h3 class="font-semibold text-sm flex items-center justify-between">Transaction Summary
    <span><%= payment_txn_status_badge(txn.status) %></span>
  </h3>
  <div class="text-sm">
    <div class="flex justify-between"><span class="text-gray-500">Txn ID:</span><span class="font-medium"><%= txn.onepay_transaction_id.presence || '—' %></span></div>
    <div class="flex justify-between"><span class="text-gray-500">Amount:</span><span class="font-medium"><%= format_vnd_currency(txn.amount.to_i) %></span></div>
    <div class="flex justify-between"><span class="text-gray-500">Created:</span><span class="font-medium"><%= format_datetime(txn.created_at) %></span></div>
    <div class="flex justify-between"><span class="text-gray-500">Category:</span><span class="font-medium"><%= payment_transaction_category_label(txn) %></span></div>
  </div>
</div>
