<div style="background-color: #f8fafc; border-radius: 0.5rem; padding: 2rem; max-width: 480px; margin: 0 auto; font-family: sans-serif; color: #1e293b;">
  <h2 style="color: #2563eb; font-size: 1.5rem; font-weight: bold; margin-bottom: 1rem;">Hello <%= @email %>!</h2>
  <% if @resource.try(:unconfirmed_email?) %>
    <p>We're contacting you to notify you that your email is being changed to <span style="font-weight: 600;"><%= @resource.unconfirmed_email %></span>.</p>
  <% else %>
    <p>We're contacting you to notify you that your email has been changed to <span style="font-weight: 600;"><%= @resource.email %></span>.</p>
  <% end %>
</div>
